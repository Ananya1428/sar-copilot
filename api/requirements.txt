fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg[binary]>=3.1,<4.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
typer>=0.12,<1.0
faker>=25.0
numpy>=1.26
redis>=5.0
scikit-learn>=1.4
networkx>=3.2
scipy>=1.11
jinja2>=3.1

# Part 8a auth (blueprint §11.3) — password hashing + JWT issuance. bcrypt
# used directly (not via passlib, which is unmaintained and warns against
# bcrypt>=4.1); PyJWT chosen over python-jose for a single HS256 use case.
bcrypt>=4.1,<5.0
pyjwt>=2.8,<3.0

# Part 5 semantic entailment check (blueprint §14.5) — a small local NLI
# model. CPU-capable; runs on GPU automatically when available (see
# domain/verification/semantic.py's device selection).
torch>=2.2,<3.0
transformers>=4.40,<5.0

# dev/test — kept in the main image for Part 1 simplicity so
# `docker compose exec api pytest` works without a separate build target.
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
